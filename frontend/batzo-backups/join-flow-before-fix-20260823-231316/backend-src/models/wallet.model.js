const WalletModel = {
  fields: {
    userId: "string",
    balance: "number",
    winningBalance: "number",
    bonusBalance: "number",
    updatedAt: "datetime"
  }
};

module.exports = WalletModel;
